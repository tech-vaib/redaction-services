# requirements.txt — Production dependencies (pip install -r requirements.txt)
# ─────────────────────────────────────────────────────────────────────────────
# Detection
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354
spacy>=3.7,<4.0
# After install: python -m spacy download en_core_web_lg
# Transformer model (optional, higher accuracy):
# pip install transformers torch
# presidio-analyzer[transformers]

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7
pydantic-settings>=2.3

# Async
anyio>=4.4
httpx>=0.27

# Observability
prometheus-client>=0.20
structlog>=24.2
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24
opentelemetry-instrumentation-fastapi>=0.45b0

# Resilience
tenacity>=8.3

# Config
pyyaml>=6.0
python-dotenv>=1.0
